---
title: useTheme
slug: /hooks/usetheme
---

# useTheme

Custom React hook: useTheme

## Hook Information

**Category:** custom

**Type:** Custom Hook


## Return Type

**Type:** `any`

## Hook Usage in Codebase

Found **4 usage(s)** across your codebase:

### Usage 1

**File:** `src\components\darkmoode\ThemeToggle.tsx` (line 7)

**Context:** Used in `ThemeToggle` function

```typescript
const { isDarkMode, toggleDarkMode } = useTheme();
```

**Destructured variables:** `isDarkMode`, `toggleDarkMode`

### Usage 2

**File:** `src\features\dashboard\components\DashboardLayout.tsx` (line 18)

**Context:** Used in `DashboardLayout` function

```typescript
const { isDarkMode, toggleDarkMode, mounted } = useTheme();
```

**Destructured variables:** `isDarkMode`, `toggleDarkMode`, `mounted`

### Usage 3

**File:** `src\features\settings\components\ThemeToggle.tsx` (line 7)

**Context:** Used in `ThemeToggle` function

```typescript
const { isDarkMode, toggleDarkMode } = useTheme();
```

**Destructured variables:** `isDarkMode`, `toggleDarkMode`

### Usage 4

**File:** `src\shared\ui\toggle\ThemeToggle.tsx` (line 17)

**Context:** Used in `ThemeToggle` function

```typescript
const { isDarkMode, toggleDarkMode } = useTheme();
```

**Destructured variables:** `isDarkMode`, `toggleDarkMode`



## Real Usage Examples

### Usage 1

```typescript
const { isDarkMode, toggleDarkMode } = useTheme();
```

### Usage 2

```typescript
const { isDarkMode, toggleDarkMode, mounted } = useTheme();
```

## Source
`src\components\darkmoode\ThemeProvider.tsx`