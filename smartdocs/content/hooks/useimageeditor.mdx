---
title: useImageEditor
slug: /hooks/useimageeditor
---

# useImageEditor

Custom React hook: useImageEditor

## Hook Information

**Category:** custom

**Type:** Custom Hook


## Return Type

**Type:** `any`

## Hook Usage in Codebase

Found **1 usage(s)** across your codebase:

### Usage 1

**File:** `src\features\create\components\CreatePost.tsx` (line 40)

**Context:** Used in `fileInputRef` function

```typescript
const {
```

**Destructured variables:** `editedImage`, `aspectRatio`, `filter`, `settings`, `loadImage`, `updateAspectRatio`, `updateFilter`, `updateBrightness`, `updateContrast`, `updateSaturation`, `updateTemperature`



## Real Usage Examples

### Usage 1

```typescript
const { 
    editedImage, 
    aspectRatio, 
    filter, 
    settings,
    loadImage, 
    updateAspectRatio, 
    updateFilter,
    updateBrightness,
    updateContrast,
    updateSaturation,
    updateTemperature,
  } = useImageEditor();
```

## Source
`src\features\create\hooks\useImageEditor.ts`