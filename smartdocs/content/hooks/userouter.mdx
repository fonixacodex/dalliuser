---
title: useRouter
slug: /hooks/userouter
---

# useRouter

Hook: useRouter (used in codebase but definition not found)

## Hook Information

**Category:** custom

**Type:** Custom Hook


## Hook Usage in Codebase

Found **7 usage(s)** across your codebase:

### Usage 1

**File:** `src\app\page.tsx` (line 10)

**Context:** Used in `router` function

```typescript
const router = useRouter();
```

### Usage 2

**File:** `src\features\auth\hooks\useAuth.ts` (line 10)

**Context:** Used in `router` function

```typescript
const router = useRouter();
```

### Usage 3

**File:** `src\features\dashboard\components\LogoutButton.tsx` (line 8)

**Context:** Used in `router` function

```typescript
const router = useRouter();
```

### Usage 4

**File:** `src\app\dashboard\tabs\@explore\error.tsx` (line 6)

**Context:** Used in `router` function

```typescript
const router = useRouter();
```

### Usage 5

**File:** `src\app\dashboard\tabs\@feed\error.tsx` (line 6)

**Context:** Used in `router` function

```typescript
const router = useRouter();
```

### Usage 6

**File:** `src\app\dashboard\tabs\@notifications\error.tsx` (line 6)

**Context:** Used in `router` function

```typescript
const router = useRouter();
```

### Usage 7

**File:** `src\app\dashboard\tabs\@profile\error.tsx` (line 6)

**Context:** Used in `router` function

```typescript
const router = useRouter();
```



## Source
`Unknown`