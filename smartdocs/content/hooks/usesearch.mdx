---
title: useSearch
slug: /hooks/usesearch
---

# useSearch

Custom React hook: useSearch

## Hook Information

**Category:** custom

**Type:** Custom Hook


## Return Type

**Type:** `any`

## Hook Usage in Codebase

Found **1 usage(s)** across your codebase:

### Usage 1

**File:** `src\features\explore\components\SearchBar.tsx` (line 11)

**Context:** Used in `SearchBar` function

```typescript
const { query, isTyping, handleChange, handleClear } = useSearch(onSearch, 500);
```

**Destructured variables:** `query`, `isTyping`, `handleChange`, `handleClear`

**Parameters used:** `onSearch`, `{NumericLiteral}`

**Default values:**

- `variable`: `onSearch`
- `initialState`: `{NumericLiteral}`



## Real Usage Examples

### Usage 1

```typescript
const { query, isTyping, handleChange, handleClear } = useSearch(onSearch, 500);
```

## Source
`src\features\explore\hooks\useSearch.ts`